# 学习打卡助手 · 免费部署到 GitHub Pages 教程（手机/平板也能用）

> 目标：把 `StudyCheckinAssistant.html` 变成一个永久免费的网址，
> 你的朋友用 **iPhone / iPad / Android 手机或平板**的浏览器打开网址就能打卡，数据存在她自己的设备上。
> 全程约 5~10 分钟，注册免费、托管永久免费，**不需要买域名、不需要服务器**。

---

## 一、为什么要"网址"，而不是直接发文件？

- 手机（尤其 **iPhone/iPad**）直接打开本地 `.html` 文件，浏览器**禁止写入存储**，会出现"今天打卡、明天记录全没了"。
- 部署成网址后，网页就能正常读写浏览器本地存储，打卡记录才能长期保留。
- 提醒：每个人的记录存在**各自的设备**上，互不相通，这是"个人版"的设计，符合隐私要求。

---

## 二、第 1 步：注册 GitHub 账号（约 2 分钟）

1. 电脑浏览器打开 **https://github.com** ，点右上角 **Sign up**。
2. 依次填写：
   - Email（建议常用邮箱，要收验证邮件）
   - Password（至少 8 位，最好字母+数字+符号）
   - Username（用户名，会出现在网址里，例如 `xiaoming`）
   - 是否接收产品邮件那一步选 **n** 即可
3. 完成人机验证（拼图题），点 **Create account**。
4. 去邮箱点 GitHub 发来的验证链接（没收到就点 Resend / 检查垃圾箱）。

> 提示：全程是英文界面，对照上面文字操作即可；如果打不开 github.com，是网络问题，可刷新或换浏览器再试。

---

## 三、第 2 步：新建仓库（Repository）

1. 登录后点右上角 **+** → **New repository**。
2. 填写：
   - Repository name：`study-checkin`（仓库名，会出现在网址里）
   - 可见性选 **Public**（公开，Pages 免费部署必需；不含隐私数据可放心）
   - **不要**勾选 "Add a README file"
3. 点绿色按钮 **Create repository**。

---

## 四、第 3 步：上传网页文件（关键一步：先重命名）

> ⚠️ 为了让朋友访问最简洁的网址，先把文件改名为 **index.html** 再上传。

1. 在你电脑上，把 `StudyCheckinAssistant.html` **重命名为 `index.html`**。
2. 回到刚建好的仓库页面，点 **Add file → Upload files**。
3. 把 `index.html` 拖进虚线框（或点 choose your files 选择）。
4. 点绿色按钮 **Commit changes**。

---

## 五、第 4 步：开启 GitHub Pages（一次设置，永久生效）

1. 在仓库页点顶部菜单 **Settings**（齿轮图标）。
2. 左侧栏往下找到 **Pages**（在 "Code and automation" 分组里）。
3. 在 **Build and deployment → Branch** 处：
   - 选分支 **main**
   - 右边目录保持 **/ (root)**
   - 点 **Save**
4. 页面刷新后顶部出现提示 "Your site is live at ..."，**等 1~2 分钟**让网站生效。

---

## 六、你的专属网址（记下来）

网址格式：

```
https://你的用户名.github.io/study-checkin/
```

例如用户名是 `xiaoming`，网址就是：

```
https://xiaoming.github.io/study-checkin/
```

1. 先用电脑浏览器打开这个网址验证能正常显示"学习打卡助手"。
2. 把网址发给朋友，她在手机/平板上点开即可使用。
3. 她可以在浏览器菜单里选 **"添加到主屏幕"**，以后像 App 一样一键打开。

---

## 七、以后如何更新版本？

以后想换新版本时，重新上传同名文件覆盖即可：

1. 打开仓库页 → **Add file → Upload files**
2. 拖入新的 `index.html`（必须同名）
3. **Commit changes**，等 1~2 分钟，网址内容自动变成新版
4. 老用户不用重新下载，打开网址就是新版（数据不丢，因为存在她设备上）

---

## 八、常见问题

**Q1：打不开 github.com 或 github.io 很慢？**
国内网络访问 GitHub 时好时坏。可换时间段/换网络再试；若长期很慢，可改用国内的 **Gitee Pages**（见文末备用方案），或让我帮你换 Cloudflare Pages 的教程。

**Q2：朋友的记录会丢吗？**
只要她不清除浏览器数据、不用"无痕模式"，记录就一直在她设备的浏览器里。
建议她定期点页面上「⚙️ 设置 → 导出备份」，把备份 JSON 存到手机相册/网盘，以防万一。

**Q3：我和她的数据会串吗？**
不会。各自设备存各自的数据，互不影响；同时用也没问题。

**Q4：需要买域名吗？**
不需要，`github.io` 自带免费域名和 HTTPS。

**Q5：上传时报错？**
- 提示文件太大：本文件不到 30KB，不会超限。
- 提示 "Add a README" 之类：忽略即可。
- Commit 按钮灰色：确认已选择文件后再点。

---

## 备用方案：国内访问更快 → Gitee（码云）Pages

如果朋友在国内且 GitHub 打开太慢，可以改用 Gitee Pages：

1. 注册 **https://gitee.com**（需手机号实名认证）。
2. 新建仓库（公开），上传 `index.html`。
3. 仓库页 → **服务 → Gitee Pages** → 选择分支 `master` → 启动。
   （首次启动可能需要平台人工审核，稍等后生效）
4. 得到网址 `https://用户名.gitee.io/study-checkin/`。

> 建议先按 GitHub 教程部署（步骤最简单），若实测朋友那边访问太慢，再考虑 Gitee。

---

## 九、把它变成"桌面 App"（独立窗口 + 专属图标 + 离线可用）

这一版新增了 3 类"App 标配"文件，需要**重新上传到仓库**：

| 文件 | 作用 | 类型 |
|---|---|---|
| `index.html` | 主程序（已加 manifest/图标引用、离线注册） | 覆盖旧文件 |
| `manifest.json` | 告诉手机这是独立 App（无地址栏） | 新增 |
| `sw.js` | 离线缓存，断网也能打开 | 新增 |
| `icon-512.png`、`icon-192.png` | Android 桌面图标 | 新增 |
| `apple-touch-icon.png` | iPhone/iPad 桌面图标 | 新增 |

**上传步骤**：仓库页 → **Add file → Upload files** → 把这 6 个文件一起拖进去
（`index.html` 同名会自动覆盖）→ **Commit changes** → 等 1~2 分钟。

### iPhone / iPad 添加到主屏幕

1. 一定要用 **Safari** 打开网址（Chrome 在 iOS 上不支持添加到主屏幕）：
   `https://mlflive.github.io/study-checkin/`
2. 点底部中间的 **分享** 按钮（方框 + 向上箭头）
3. 在弹出的菜单里向下滑，找到 **添加到主屏幕**（图标是个加号）
4. 名称默认是"学习打卡"，点右上角 **添加**
5. 返回桌面，图标已生成；**从图标打开是全屏、没有地址栏**，和真 App 一样

### Android 添加到主屏幕

1. 用 **Chrome / Edge** 打开同一个网址
2. 点右上角 **⋮** → **添加到主屏幕**（部分机型显示为"安装应用 / Install app"）
3. 点 **添加 / 安装**，桌面出现图标，打开即独立窗口

### 图标没更新 / 还是旧图标？

- **iOS 缓存很顽固**：长按桌面图标 → 删除书签 → 在 Safari 里清一次缓存
  （设置 → Safari → 清除历史记录与网站数据）→ 再重新"添加到主屏幕"。
- **Android**：长按图标 → 移除 → 重新添加。
- 注意：清除 Safari 网站数据会**清掉打卡记录**！如果她已经在用，请先让她在页面里
  「⚙️ → 导出备份」，清完缓存重新添加后再「导入备份」。

### 以后改网页，手机上如何强制更新？

改完 `index.html` 上传后如果打开还是旧版：
把 `sw.js` 里的

```js
const CACHE = 'study-checkin-v1';
```

改成 `'study-checkin-v2'`、`'v3'`…… 一起上传，就会强制刷新缓存。

